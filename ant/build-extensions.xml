<?xml version="1.0" encoding="UTF-8"?>
<project default="all" basedir="." name="LuceeExtensions">

<scriptdef name="readRequiredExtensions" language="javascript" classpath="${nashorn_classpath}">
	<attribute name="file" />
	<![CDATA[
		project.log("--- Read Require-Extension from "+attributes.get( "file" )+" ---");
		var filename = attributes.get( "file" );
		var manifest = new java.util.jar.Manifest( new java.io.FileInputStream( new java.io.File( filename ) ) );
		var attrs = manifest.getMainAttributes();
		var str = attrs.getValue( "Require-Extension" );
		project.log("--- RAW ---");
		project.log(str);
		project.log("------");
		var arr = str.split( "," );
		var count = 0;
		var ids = "";
		var data = "";
		var versions = "";
		var labels = "";
		for (var i = 0 ; i < arr.length ; i++ ) {
			var line = arr[ i ];
			project.log("- "+line);
			var extArr = line.split( ';' );
			
			if ( extArr.length < 1 ) 
				continue;
			count++;
			// the first is the id
			if ( ids.length >0 )
				ids += ",";
			ids += extArr[ 0 ];
			if ( data.length > 0 )
				data+=",";
			var strGAV = extArr[ 0 ];
			var _id = extArr[ 0 ];
			
			project.setProperty( "reqExt.ID" + ( i + 1 ), _id );

			// the rest has names
			var _version="";
			var _name="";
			var group="";
			var groupPath="";
			var artifact="";

			// GAV
			var gav = strGAV.split( ':' );
			if ( gav.length > 2 ) {
				_name=gav[1].trim();
				group=gav[0].trim();
				groupPath=group.replace('.','/');
				artifact=gav[1].trim();
				_id = group+"-"+artifact;
				var names=_name.split( '-' );
				if(names.length>1) {
					var tmp="";
					for(var x=0;x<names.length;x++) {
						tmp+=names[x][0].toUpperCase()+names[x].substring(1);
					}
					_name=tmp;
				}

				_version=gav[2].trim();
			}
			data += _id+"-"+_version;

			if(extArr.length>1) {
				for (var y = 1; y < extArr.length ; y++ ) {
					var ext = extArr[ y ];
					var index = ext.indexOf( '=' );
					if ( index == -1 ) 
						continue;
					var name = ext.substring( 0, index ).trim();
					var value = ext.substring( index + 1 ).trim();
					if ( name == "version" ) {
						if ( versions.length > 0)
							versions += ",";
						versions += value;
						data += ";" + value;
						_version = value;
					}
					if ( name == "label" ) {
						if( labels.length > 0)
							labels += ",";
						labels += value;
					} else if ( name == "name" ) {
						_name = value;
					}
					project.setProperty( "reqExt." + name + ( i + 1 ), value );
				}
			}


			project.log("ext" + _name + " = "+ _version);
			project.log(_id);
			
			project.setProperty( "reqExt." + _name, _version );
			project.setProperty( "ext" + _name + "Version", _version );
			project.setProperty( "ext" + _name + "Artifact", artifact );
			project.setProperty( "ext" + _name + "Group", group );
			project.setProperty( "ext" + _name + "GroupPath", groupPath );
			project.setProperty( "ext" + _name, _id );
		}
		project.setProperty( "reqExt.length", count + "" );
		project.setProperty( "reqExt.ids", ids );
		project.setProperty( "reqExt.versions", versions );
		project.setProperty( "reqExt.labels", labels );
		project.setProperty( "reqExt.data", data );

		//
	]]>
	</scriptdef>
	<!--
	<scriptdef name="splitIdVersion" language="javascript">
		<attribute name="prefix" />
		<attribute name="value" />
		<![CDATA[
			var prefix = attributes.get( "prefix" );
			var value = attributes.get( "value" );
			
			var index = value.indexOf( ";" );
			var id;
			var version="";
			// no version
			if ( index ==-1 ) {
				id = value.trim();
			} else {
				id = value.substring( 0, index );
				version = value.substring( index + 1 );
			}
			project.setProperty( prefix + "id", id );
			project.setProperty( prefix + "version", version );
		]]>
	</scriptdef>
	-->

	<scriptdef name="toFileName" language="javascript" classpath="${nashorn_classpath}">
		<attribute name="id" />
		<attribute name="name" />
		<attribute name="version" />
		<attribute name="property" />
		<![CDATA[
			var id = attributes.get( "id" ).trim();
			var name = attributes.get( "name" ).trim();
			var version = attributes.get( "version" ).trim();
			
			var needle = ' ';
			//var regex = new RegExp( needle, 'g' );
			var repl = '-';
		
			// removes quotes
			if ( name.startsWith( '"' ) && name.endsWith( '"' ) ) {
				name = name.substring( 1, name.length() -1 ).trim();
			}
			if ( id.startsWith( '"' ) && id.endsWith( '"' ) ) {
				id = id.substring( 1 , id.length() -1 ).trim();
			}

			// remove brackets
			var start, end;
			while ( (start = name.indexOf( '(' ) ) != -1 ) {
				end = name.indexOf( ')', start + 1 );
				if( end == -1) 
					break;
				name = ( name.substring( 0, start ) + name.substring( end +1 ) ).trim();
			}

			// version
			if ( version.startsWith( '"' ) && version.endsWith( '"' ) ) {
				version = version.substring( 1, version.length() - 1 ).trim();
			}

			name=name.trim();
			name=name.replace( " " , "-" ).toLowerCase();
			name=name.replace( " " , "-" ); // TODO make better but not with regex some ant version will fail
			name=name.replace( " " , "-" );
			name=name.replace( " " , "-" );
			name=name.replace( " " , "-" );
			name=name.replace( " " , "-" );

			project.setProperty( attributes.get( "property" ), id + "-" + version + ".lex" );
		]]>
	</scriptdef>

	<target name="check-cache">
		<available file="${cache}/testbox-${testboxVersion}.zip" property="testbox.present"/>

		<available file="${cache}/${extMysqlJdbcExtension}-${extMysqlJdbcExtensionVersion}.lex" property="mysql.present"/>
		<available file="${cache}/${extMssqlJdbcExtension}-${extMssqlJdbcExtensionVersion}.lex" property="mssql.present"/>
		<available file="${cache}/${extPostgresqlJdbcExtension}-${extPostgresqlJdbcExtensionVersion}.lex" property="postgre.present"/>
		<available file="${cache}/${extJtdsJdbcExtension}-${extJtdsJdbcExtensionVersion}.lex" property="jdts.present"/>
		<available file="${cache}/${extS3Extension}-${extS3ExtensionVersion}.lex" property="s3.present"/>
		<available file="${cache}/${extAdministratorExtension}-${extAdministratorExtensionVersion}.lex" property="admin.present"/>
		<available file="${cache}/${extDocumentationExtension}-${extDocumentationExtensionVersion}.lex" property="doc.present"/>
		<available file="${cache}/${extPdfExtension}-${extPdfExtensionVersion}.lex" property="pdf.present"/>
		<available file="${cache}/${extImageExtension}-${extImageExtensionVersion}.lex" property="image.present"/>
		<available file="${cache}/${extEsapiExtension}-${extEsapiExtensionVersion}.lex" property="esapi.present"/>
		<available file="${cache}/${extSchedulerClassicExtension}-${extSchedulerClassicExtensionVersion}.lex" property="scheduler.present"/>
		<available file="${cache}/${extCompressExtension}-${extCompressExtensionVersion}.lex" property="compress.present"/>
	</target>

	<target name="check-mysql" depends="check-cache" unless="mysql.present">
		<get 
			src="${extURLMaven}${extMysqlJdbcExtensionGroupPath}/${extMysqlJdbcExtensionArtifact}/${extMysqlJdbcExtensionVersion}/${extMysqlJdbcExtensionArtifact}-${extMysqlJdbcExtensionVersion}.lex" 
			dest="${cache}/${extMysqlJdbcExtension}-${extMysqlJdbcExtensionVersion}.lex"/>
	</target> 
	
	<target name="check-mssql" depends="check-cache" unless="mssql.present">
		<get 
			src="${extURLCDN}${extMssqlJdbcExtensionGroupPath}/${extMssqlJdbcExtensionArtifact}/${extMssqlJdbcExtensionVersion}/${extMssqlJdbcExtensionArtifact}-${extMssqlJdbcExtensionVersion}.lex" 
			dest="${cache}/${extMssqlJdbcExtension}-${extMssqlJdbcExtensionVersion}.lex"/>
	</target> 

	<target name="check-postgre" depends="check-cache" unless="postgre.present">
		<get 
			src="${extURLMaven}${extPostgresqlJdbcExtensionGroupPath}/${extPostgresqlJdbcExtensionArtifact}/${extPostgresqlJdbcExtensionVersion}/${extPostgresqlJdbcExtensionArtifact}-${extPostgresqlJdbcExtensionVersion}.lex" 
			dest="${cache}/${extPostgresqlJdbcExtension}-${extPostgresqlJdbcExtensionVersion}.lex"/>
	</target> 
	<target name="check-jdts" depends="check-cache" unless="jdts.present">
		<get 
			src="${extURLCDN}${extJtdsJdbcExtensionGroupPath}/${extJtdsJdbcExtensionArtifact}/${extJtdsJdbcExtensionVersion}/${extJtdsJdbcExtensionArtifact}-${extJtdsJdbcExtensionVersion}.lex" 
			dest="${cache}/${extJtdsJdbcExtension}-${extJtdsJdbcExtensionVersion}.lex"/>
	</target>
	<target name="check-s3" depends="check-cache" unless="s3.present">
		<get 
			src="${extURLCDN}${extS3ExtensionGroupPath}/${extS3ExtensionArtifact}/${extS3ExtensionVersion}/${extS3ExtensionArtifact}-${extS3ExtensionVersion}.lex" 
			dest="${cache}/${extS3Extension}-${extS3ExtensionVersion}.lex"/>
	</target>
	<target name="check-pdf" depends="check-cache" unless="pdf.present">
		<get 
			src="${extURLMaven}${extPdfExtensionGroupPath}/${extPdfExtensionArtifact}/${extPdfExtensionVersion}/${extPdfExtensionArtifact}-${extPdfExtensionVersion}.lex" 
			dest="${cache}/${extPdfExtension}-${extPdfExtensionVersion}.lex"/>
	</target>
	<target name="check-admin" depends="check-cache" unless="admin.present">
		<get 
			src="${extURLMaven}${extAdministratorExtensionGroupPath}/${extAdministratorExtensionArtifact}/${extAdministratorExtensionVersion}/${extAdministratorExtensionArtifact}-${extAdministratorExtensionVersion}.lex" 
			dest="${cache}/${extAdministratorExtension}-${extAdministratorExtensionVersion}.lex"/>
	</target>
	<target name="check-doc" depends="check-cache" unless="doc.present">
		<get 
			src="${extURLMaven}${extDocumentationExtensionGroupPath}/${extDocumentationExtensionArtifact}/${extDocumentationExtensionVersion}/${extDocumentationExtensionArtifact}-${extDocumentationExtensionVersion}.lex" 
			dest="${cache}/${extDocumentationExtension}-${extDocumentationExtensionVersion}.lex"/>
	</target>
	<target name="check-image" depends="check-cache" unless="image.present">
		<get 
			src="${extURLMaven}${extImageExtensionGroupPath}/${extImageExtensionArtifact}/${extImageExtensionVersion}/${extImageExtensionArtifact}-${extImageExtensionVersion}.lex" 
			dest="${cache}/${extImageExtension}-${extImageExtensionVersion}.lex"/>
	</target>
	<target name="check-esapi" depends="check-cache" unless="esapi.present">
		<get 
			src="${extURLMaven}${extEsapiExtensionGroupPath}/${extEsapiExtensionArtifact}/${extEsapiExtensionVersion}/${extEsapiExtensionArtifact}-${extEsapiExtensionVersion}.lex" 
			dest="${cache}/${extEsapiExtension}-${extEsapiExtensionVersion}.lex"/>
	</target>
	<target name="check-scheduler" depends="check-cache" unless="scheduler.present">
		<get 
			src="${extURLMaven}${extSchedulerClassicExtensionGroupPath}/${extSchedulerClassicExtensionArtifact}/${extSchedulerClassicExtensionVersion}/${extSchedulerClassicExtensionArtifact}-${extSchedulerClassicExtensionVersion}.lex" 
			dest="${cache}/${extSchedulerClassicExtension}-${extSchedulerClassicExtensionVersion}.lex"/>
	</target>
	<target name="check-compress" depends="check-cache" unless="compress.present">
		<get 
			src="${extURLMaven}${extCompressExtensionGroupPath}/${extCompressExtensionArtifact}/${extCompressExtensionVersion}/${extCompressExtensionArtifact}-${extCompressExtensionVersion}.lex" 
			dest="${cache}/${extCompressExtension}-${extCompressExtensionVersion}.lex"/>
	</target>
	<target name="check-testbox" depends="check-cache" unless="testbox.present">
		<get src="${testboxURL}" dest="${cache}/testbox-${testboxVersion}.zip"/>
	</target> 
	<target name="addExtensions" if="buildLoaderJar">
		<echots message="add extensions"/>
		
		<mkdir dir="${goal}/extensions/"/>

		<!-- add extension MySQL -->
		<loadmf jar="${cache}/${extMysqlJdbcExtension}-${extMysqlJdbcExtensionVersion}.lex" prefix="ext_mysql."/>
		<toFileName id="${ext_mysql.id}" name="${ext_mysql.name}" version="${ext_mysql.version}" property="ext_mysql.filename"/>
		<copy 
			file="${cache}/${extMysqlJdbcExtension}-${extMysqlJdbcExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extMysqlJdbcExtension}-${extMysqlJdbcExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_mysql.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_mysql.filename}</echo>
		<echo file="${goal}/extensions/.index">${ext_mysql.filename};</echo>
		

		<!-- add extension MSSQL -->
		<loadmf jar="${cache}/${extMssqlJdbcExtension}-${extMssqlJdbcExtensionVersion}.lex" prefix="ext_mssql."/>
		<toFileName id="${ext_mssql.id}" name="${ext_mssql.name}" version="${ext_mssql.version}" property="ext_mssql.filename"/>
		<copy 
			file="${cache}/${extMssqlJdbcExtension}-${extMssqlJdbcExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extMssqlJdbcExtension}-${extMssqlJdbcExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_mssql.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_mssql.filename}</echo>
		<echo file="${goal}/extensions/.index" append="true">${ext_mssql.filename};</echo>
		
		<!-- add extension Postgre -->
		<loadmf jar="${cache}/${extPostgresqlJdbcExtension}-${extPostgresqlJdbcExtensionVersion}.lex" prefix="ext_postgre."/>
		<toFileName id="${ext_postgre.id}" name="${ext_postgre.name}" version="${ext_postgre.version}" property="ext_postgre.filename"/>
		<copy 
			file="${cache}/${extPostgresqlJdbcExtension}-${extPostgresqlJdbcExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extPostgresqlJdbcExtension}-${extPostgresqlJdbcExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_postgre.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_postgre.filename}</echo>
		<echo file="${goal}/extensions/.index" append="true">${ext_postgre.filename};</echo>
		
		<!-- add extension JDTS -->
		<loadmf jar="${cache}/${extJtdsJdbcExtension}-${extJtdsJdbcExtensionVersion}.lex" prefix="ext_jdts."/>
		<toFileName id="${ext_jdts.id}" name="${ext_jdts.name}" version="${ext_jdts.version}" property="ext_jdts.filename"/>
		<copy 
			file="${cache}/${extJtdsJdbcExtension}-${extJtdsJdbcExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extJtdsJdbcExtension}-${extJtdsJdbcExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_jdts.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_jdts.filename}</echo>
		<echo file="${goal}/extensions/.index" append="true">${ext_jdts.filename};</echo>

		<!-- add extension S3 -->
		<loadmf jar="${cache}/${extS3Extension}-${extS3ExtensionVersion}.lex" prefix="ext_s3."/>
		<toFileName id="${ext_s3.id}" name="${ext_s3.name}" version="${ext_s3.version}" property="ext_s3.filename"/>
		<copy 
			file="${cache}/${extS3Extension}-${extS3ExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extS3Extension}-${extS3ExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_s3.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_s3.filename}</echo>
		<echo file="${goal}/extensions/.index" append="true">${ext_s3.filename};</echo>

		<!-- add extension PDF -->
		<loadmf jar="${cache}/${extPdfExtension}-${extPdfExtensionVersion}.lex" prefix="ext_pdf."/>
		<toFileName id="${ext_pdf.id}" name="${ext_pdf.name}" version="${ext_pdf.version}" property="ext_pdf.filename"/>
		<copy 
			file="${cache}/${extPdfExtension}-${extPdfExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extPdfExtension}-${extPdfExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_pdf.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_pdf.filename}</echo>
		<echo file="${goal}/extensions/.index" append="true">${ext_pdf.filename};</echo>
		
		<!-- add extension Admin -->
		<loadmf jar="${cache}/${extAdministratorExtension}-${extAdministratorExtensionVersion}.lex" prefix="ext_admin."/>
		<toFileName id="${ext_admin.id}" name="${ext_admin.name}" version="${ext_admin.version}" property="ext_admin.filename"/>
		<copy 
			file="${cache}/${extAdministratorExtension}-${extAdministratorExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extAdministratorExtension}-${extAdministratorExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_admin.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_admin.filename}</echo>
		<echo file="${goal}/extensions/.index" append="true">${ext_admin.filename};</echo>
		
		<!-- add extension Doc -->
		<loadmf jar="${cache}/${extDocumentationExtension}-${extDocumentationExtensionVersion}.lex" prefix="ext_doc."/>
		<toFileName id="${ext_doc.id}" name="${ext_doc.name}" version="${ext_doc.version}" property="ext_doc.filename"/>
		<copy 
			file="${cache}/${extDocumentationExtension}-${extDocumentationExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extDocumentationExtension}-${extDocumentationExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_doc.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_doc.filename}</echo>
		<echo file="${goal}/extensions/.index" append="true">${ext_doc.filename};</echo>

		<!-- add extension Image -->
		<loadmf jar="${cache}/${extImageExtension}-${extImageExtensionVersion}.lex" prefix="ext_image."/>
		<toFileName id="${ext_image.id}" name="${ext_image.name}" version="${ext_image.version}" property="ext_image.filename"/>
		<copy 
			file="${cache}/${extImageExtension}-${extImageExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extImageExtension}-${extImageExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_image.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_image.filename}</echo>
		<echo file="${goal}/extensions/.index" append="true">${ext_image.filename};</echo>

		<!-- add extension Esapi -->
		<loadmf jar="${cache}/${extEsapiExtension}-${extEsapiExtensionVersion}.lex" prefix="ext_esapi."/>
		<toFileName id="${ext_esapi.id}" name="${ext_esapi.name}" version="${ext_esapi.version}" property="ext_esapi.filename"/>
		<copy 
			file="${cache}/${extEsapiExtension}-${extEsapiExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extEsapiExtension}-${extEsapiExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_esapi.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_esapi.filename}</echo>
		<echo file="${goal}/extensions/.index" append="true">${ext_esapi.filename};</echo>

		<!-- add extension Scheduler -->
		<loadmf jar="${cache}/${extSchedulerClassicExtension}-${extSchedulerClassicExtensionVersion}.lex" prefix="ext_scheduler."/>
		<toFileName id="${ext_scheduler.id}" name="${ext_scheduler.name}" version="${ext_scheduler.version}" property="ext_scheduler.filename"/>
		<copy 
			file="${cache}/${extSchedulerClassicExtension}-${extSchedulerClassicExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extSchedulerClassicExtension}-${extSchedulerClassicExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_scheduler.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_scheduler.filename}</echo>
		<echo file="${goal}/extensions/.index" append="true">${ext_scheduler.filename};</echo>

		<!-- add extension Compress -->
		<loadmf jar="${cache}/${extCompressExtension}-${extCompressExtensionVersion}.lex" prefix="ext_compress."/>
		<toFileName id="${ext_compress.id}" name="${ext_compress.name}" version="${ext_compress.version}" property="ext_compress.filename"/>
		<copy 
			file="${cache}/${extCompressExtension}-${extCompressExtensionVersion}.lex" 
			todir="${goal}/extensions/"/>
		<move 
			file="${goal}/extensions/${extCompressExtension}-${extCompressExtensionVersion}.lex" 
			tofile="${goal}/extensions/${ext_compress.filename}"/>
		<echo level="info">add:${goal}/extensions/${ext_compress.filename}</echo>
		<echo file="${goal}/extensions/.index" append="true">${ext_compress.filename};</echo>

	</target>

	<target name="check-lucee-loader-jar">
		<available file="${cache}/${stableLoader}.jar" property="lucee.loader-jar.present"/>
	</target>

	<target name="download-lucee-loader-jar" unless="lucee.loader-jar.present">
		<mkdir dir="${cache}"/>
		<get src="${cdnUrl}${stableLoader}.jar" dest="${cache}/${stableLoader}.jar"/>
	</target>

	<scriptdef name="copyExtensions" language="javascript" classpath="${nashorn_classpath}">
		<attribute name="source" />
		<attribute name="target" />
		<attribute name="extensions" />
		<![CDATA[

			var sourceDir = attributes.get( "source" );
			var targetDir = attributes.get( "target" );
			var extensions = attributes.get( "extensions" ).split(",");
			if (extensions.length == 0 )
				throw ( "copyExtensions: no extensions specified?" );

			var copy = project.createTask( "copy" );
			var echo = project.createTask( "echo" );

			// echo.setMessage( "copying Extensions to : " + targetDir );
			// echo.perform();	

			for (var e in extensions){
				var extFile = extensions[e].split(";").join("-") + ".lex";
				// echo.setMessage( "copy Extension: " + sourceDir + "/" + extFile );
				// echo.perform();	
				copy.setFile( new java.io.File( sourceDir + "/" + extFile ) );
				copy.setTofile( new java.io.File( targetDir + "/" + extFile ) );
				copy.perform();
			}
		]]>
	</scriptdef>

</project>