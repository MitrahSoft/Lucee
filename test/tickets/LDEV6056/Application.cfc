component {
	this.name = "LDEV6056";
	this.mappings[ "/coldbox" ] = getDirectoryFromPath( getCurrentTemplatePath() ) & "coldbox";
	this.mappings[ "/preside" ] = getDirectoryFromPath( getCurrentTemplatePath() ) & "preside";
}
